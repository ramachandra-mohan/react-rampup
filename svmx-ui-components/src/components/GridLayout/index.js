/**
 * The SVMX UI Layout module exports the 'grid layout' for SVMX UI. One can import
 * the components as below.
 *
 * Eg.
 *  import {GridLayout, Layout} from 'components/GridLayout'
 */
import GridLayout from './GridLayout';
import Layout from './Layout';
export { GridLayout, Layout };
